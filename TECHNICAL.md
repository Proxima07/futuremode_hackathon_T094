# SnapFit 技術文件

FUTUREMODE 2026 · 隊伍 T094
儲存庫：https://github.com/Proxima07/futuremode_hackathon_T094

這份文件從架構講起，然後說明每一層的細節，
最後是開發過程中實測發現的問題與對應的解法。

---

## 目錄

1. [一句話的技術命題](#一句話的技術命題)
2. [架構總覽](#架構總覽)
3. [兩層分工](#兩層分工)
4. [本機視覺層](#本機視覺層)
5. [三條遠端路徑](#三條遠端路徑)
6. [收斂機制：模型提案，程式決定](#收斂機制模型提案程式決定)
7. [驗證層：不信任模型輸出](#驗證層不信任模型輸出)
8. [實測發現的問題與解法](#實測發現的問題與解法)
9. [容錯設計](#容錯設計)
10. [效能數字與測試](#效能數字與測試)

---

## 一句話的技術命題

**把一個「每秒都在變」的畫面，變成一個「使用者跟得上」的指示。**

這句話拆開來有三個互相衝突的需求：

- 畫面每秒 30 幀，使用者的手一直在動
- 視覺語言模型的延遲是幾百毫秒到數秒，而且會變動
- 人讀完一句話、抬頭、動手、再低頭確認，需要好幾秒

如果直接把模型的每一次回應丟到畫面上，結果會是：
框一直跳、提示一直換、使用者永遠追不上。

**整個系統的設計都是為了解決這個落差。**

---

## 架構總覽

```
┌──────────────────────── 手機瀏覽器 ────────────────────────┐
│                                                            │
│  MediaStream（多鏡頭切換 / 兩指縮放 / 串流看門狗）             │
│      │                                                     │
│      ├── 本機層 · 零延遲 ──────────────────────────┐        │
│      │   exposure.js       亮度、反差、色溫、飽和、色相│       │
│      │   frameFreshness.js 32×32 指紋比對，容忍手震    │       │
│      │   overlayCanvas.js  構圖線 + 目標框 + 暗化遮罩   │       │
│      │   planStabilizer.js 多幀共識，決定何時鎖定與放行 │       │
│      └───────────────────────────────────────────┘        │
│      │                                                     │
│      └── 遠端層 · 三條路徑，各有節奏 ──────────────┐        │
│          /api/plan    約每秒一次，且畫面要有變化      │        │
│          /api/light   由光線變化觸發，最短 15 秒      │        │
│          /api/custom  內建版型不合用時，冷卻 15 秒     │        │
└──────────────────┼─────────────────────────────────┘        │
                   ↓  HTTPS（Cloudflare Tunnel）
    ┌──────────────────────────────────────────┐
    │  FastAPI（單一 port，API 與靜態同源）        │
    │                                           │
    │  routers/plan.py    三個端點                │
    │  services/prompts   三組提示詞 + 情境指引    │
    │  services/validate  驗證與修正模型輸出       │
    │  services/chain     Provider 降級鏈         │
    └───────────────────┬──────────────────────┘
                        ↓
      國網中心 gemma-4-26B-A4B-it
        └─ 失敗 → GMI Cloud → OpenAI → fallback（畫面維持現狀）
```

**沒有資料庫。** 照片不上傳、不儲存，
全部在瀏覽器記憶體處理完就釋放。

**前後端同源。** FastAPI 同時服務 `/api/*` 與 `web/` 靜態檔案，
所以沒有 CORS 問題，Cloudflare Tunnel 也只要開一條。

**前端沒有 build 步驟。** 純原生 ES modules，
整個專案複製到樹莓派就能跑，不需要在目標機器上跑 npm。

** 實際配置路徑 ** 
配置在樹梅派上，該樹梅派裝有中華電信家用網路實體連線。
透過 Cloudflare tunnel 進行流量轉接，並將網域所有權保持在Cloudflare上，添加CNAME的紀錄 已進行流量轉址。

---

## 兩層分工

**本機負責即時，遠端負責語意。**
判斷一件事該放哪一層，只問一個問題：**這需要理解畫面內容嗎？**

| | 本機 | 遠端 |
| --- | --- | --- |
| 延遲 | 0 | 300ms ~ 數秒 |
| 頻率 | 每 250ms | 每秒最多一次 |
| 回答 | 亮度多少、畫面有沒有動、框畫在哪 | 這是什麼、哪個是主角、該往哪移 |
| 失效時 | 產品不能用 | 產品變笨但仍可用 |

畫面上的框、輔助線、對齊狀態全部在本機重畫，**不等網路**。
所以即使模型逾時，畫面也不會凍住或閃爍。

---

## 本機視覺層

### 曝光統計（`web/js/vision/exposure.js`）

把畫面降到 64×64 之後算三類數字：

**亮度**

```
subject         目標框「內」的平均亮度
background      目標框「外」的平均亮度
subject_ratio   主體 ÷ 背景
mean / clipped_high / clipped_low / left / right / top / bottom
```

`subject_ratio` 是判斷背光的客觀指標。
背光的定義本來就是「主體比背景暗很多」，
有這個比值模型就不用猜（低於 0.55 基本上就是背光）。

**反差**

`contrast` 是亮度的標準差。
高代表明暗落差大（硬光），低代表平光。
少了這個數字，「光太硬」和「光太平」也只能靠模型猜。

**顏色**

```
warmth          -1 冷（藍）~ +1 暖（橙）
saturation      平均飽和度
hue_spread      色相散布 0~1，高代表多種顏色的光混雜
dominant_hue    主色相 0~360
```

驗證過能正確分辨：

| 場景 | warmth | 飽和度 | 色相 |
| --- | --- | --- | --- |
| 夜店藍光 | −0.739 | 0.85 | 229° |
| 餐廳暖黃 | +0.438 | 0.61 | 34° |
| 中性白光 | −0.012 | 0.02 | — |
| 粉紅霓虹 | +0.222 | 0.59 | 337° |

**為什麼要算顏色：** 少了這些數字，模型分不出「白平衡跑掉」
和「這是霓虹場景」。前者要修，後者是風格。

### 可見範圍裁切

`video` 用 `object-fit: cover` 顯示，兩側或上下會被裁掉。

`visibleRegion()` 算出原始影格中實際看得到的區塊，
**`grabFrame` 與 `measure` 都只取這一塊**。

不做這件事的話，模型會分析到螢幕上根本沒有的東西，
給出的建議就對不上使用者看到的畫面。

實測影響不小：960×1080 的影片放進手機直式畫面，
**有 37% 的區域是使用者看不到、但模型一直在分析的。**

### 畫面比對（`web/js/vision/frameFreshness.js`）

把畫面降到 32×32 灰階當指紋，回答一個問題：
**這兩張是不是同一個場景。**

比對時做兩件補償：

1. **整體平移**：在 ±3 個縮圖像素（約畫寬 9%）的範圍內搜尋最佳對位
2. **曝光增益**：補償自動曝光造成的整體亮度跳動

這不是物件追蹤，只是「畫面新鮮度」的近似檢查。
「是否真的對準」由模型判斷。

---

## 三條遠端路徑

拆分的依據是**變化速度**，不是平行度。

| 端點 | 內容 | 節奏 | max_tokens |
| --- | --- | --- | --- |
| `/api/plan` | 版型判斷 + 物品配置 + 移動指示 | 約每秒一次，畫面有變化才送 | 280 / 360 |
| `/api/light` | 環境判斷 + 光線建議 | 由光線變化觸發，最短 15 秒 | 160 |
| `/api/custom` | 動態版型生成 | 內建版型不合用時，冷卻 15 秒 | 420 |

**三條路徑互相禮讓，不平行。**
排程器只讓一條在飛，優先序 `custom` > `light` > `plan`。
同時打同一個端點只會讓兩邊都變慢。

**計時基準是「收到回應之後」，不是「送出之時」。**
節奏自動變成「延遲 + 間隔」，後端變慢時自動放慢，
請求永遠不會堆積。

### 兩組提示詞

主路徑依階段切換：

**SEARCHING** 用 `PLAN_SYSTEM`——提供完整版型目錄，
要求選一個並給初始調整。

**GUIDING** 用 `GUIDING_SYSTEM`——**不再提供版型目錄**，
只送目前的固定座標、物品、對準部位、上一個動作，
要求回傳 `move / ready / lost`。

這個切換是刻意的。引導階段如果還給它整份目錄，
模型會忍不住重新選版型，目標就固定不下來。

後端也會強制 GUIDING 階段沿用目前版型並丟掉 `adjust` / `custom`。

---

## 收斂機制：模型提案，程式決定

**這是整個系統最重要的設計。**

視覺語言模型每次看到的畫面都略有不同，回答也會略有不同。
直接把每次回應丟到畫面上，框就會一直跳。

所以：**模型只負責提案與判斷，收斂規則由程式保證。**

實作在 `web/js/planner/planStabilizer.js`，是一個三階段狀態機：

```
SEARCHING ──── 同一版型連續 2 次提案 ────→ GUIDING
                                            │
GUIDING ──── 最近 3 次有 2 次 ready ────→ READY
                                            │
READY ──── 畫面持續變化 / 主體消失 ────→ 撤銷，回 GUIDING
```

### 各項門檻

| 參數 | 值 | 意義 |
| --- | --- | --- |
| `planConfirmations` | 2 | 同一版型與方向要連續兩次才定案 |
| `maxPlanSamples` | 3 | 三次仍不一致就用多數決，同票取最近 |
| `readyVotes` / `readyWindow` | 2 / 3 | 最近三次有兩次 ready 才顯示可拍攝 |
| `guidanceConfirmations` | 2 | 新的移動方向要連續兩次才替換提示 |
| `lostConfirmations` | 2 | 連續兩次判定主體消失才重新規劃 |
| `evidenceMaxAgeMs` | 10000 | 過期的證據不能和新影格湊票 |
| `adviceRefreshMs` | 2500 | 同方向的提示最快 2.5 秒換一次字 |

### 固定目標

一旦鎖定，這些東西全部不再變動：
版型、鏡射、翻轉、平移、縮放、交點、對準部位。

**不會因為使用者靠近了目標，就把目標再移走。**
這在早期版本發生過，使用者永遠追不上一個一直在跑的框。

### 提示的閱讀時間

`GUIDANCE_HOLD_MS: 7000`——明確的操作指示至少顯示 7 秒。

停留期間只保留最新一筆候選提示，不累積過時指令。
主體消失、到達目標、畫面大幅改變可以立即打斷。

**這只影響文字什麼時候能被換掉，不減慢背景判斷。**
模型仍然照原頻率在跑。

---

## 驗證層：不信任模型輸出

`server/services/validate.py`。原則是
**該拒絕的整份拒絕，該修正的就修正，該忽略的只忽略。**

### 通用清洗

- 剝除 markdown 圍欄；失敗時撈第一個 `{...}` 區塊再試一次
- 所有列舉值比對白名單，非法值退回預設
- 空值正規化：`"null"` / `"none"` / `""` 一律轉成 `None`

最後這一項是實測踩出來的，見下一節。

### 動態版型的修正

模型生成的座標經常不合格。逐個 slot 檢查：

```
座標格式錯    → 丟掉這個 slot
座標順序顛倒  → 交換回來
超出安全區    → 夾進 y[0.12, 0.76] x[0.04, 0.96]
面積 < 2%     → 丟掉（使用者對不準）
面積 > 72%    → 丟掉（沒有引導意義）
與已接受的框 IoU > 0.55 → 丟掉後者（看不清楚）
```

收完之後再檢查整體：

```
一個 slot 都沒剩 → custom 失敗，退回內建版型
沒有主角        → 面積最大的自動升格
多個主角        → 只留面積最大的
超過 5 個位置    → 截斷
```

**修不動才退回，不是整份丟掉。**

### 壓掉自相矛盾

模型偶爾會給出互相矛盾的欄位，這些一律在後端壓掉：

- 說光線沒問題卻要求補光 → 清掉補光方向
- 說 `stylish` 卻填了 `issue` → 清掉 issue
- 說 `problem` 卻沒說是什麼問題 → 降級成 `stylish`
- 同一個物件既被指派又被要求移除 → 整份拒絕

---

## 實測發現的問題與解法

這一節是開發過程中真正花時間的部分。
每一項都是「先觀察到症狀，再定位原因，最後才動手」。

### 1 · 手震被當成明確位移

**症狀**：使用者手抖一下，引導提示就被清掉、可拍攝狀態被撤銷。

**原因**：畫面比對只容許 **±1 個縮圖像素**的整體平移。
32×32 的一個像素約是**畫寬的 3%**——手持拍攝隨便晃一下就超過。
而且是**單幀比對就下結論**，沒有連續確認。

**解法**：搜尋半徑放寬到 ±3 像素（約 9%），
再加上連續兩次確認（取樣間隔 250ms，約 0.5 秒）。

| 平移量 | 修正前 | 修正後 |
| --- | --- | --- |
| 3% | 同一畫面 | 同一畫面 |
| 6% | **判定移動** | 同一畫面 |
| 9% | **判定移動** | 同一畫面 |
| 13% | 判定移動 | 判定移動 |

真正的移動（轉向、換物件、走動）遠大於 9%，仍然抓得到。
代價是每次比較從 9 個位移試到 49 個——
32×32 只有一千個像素，即使 49 次也不到一毫秒。

參數在 `web/js/lib/config.js` 的 `MOTION`。

### 2 · 模型漏填欄位被當成主體消失

**症狀**：物品明明好好地在框裡，提示卻顯示
「把要拍的東西放到鏡頭前」。

**原因**：`GUIDING_SYSTEM` 的重點是回 `move / ready / lost`，
`placements` 只是附帶欄位，模型很容易省略。
而前端寫的是：

```javascript
if (!plan.placements?.length) alignment = "lost";
```

於是模型明明說了 `ready`，卻因為沒填 placements 被改判成 lost。

**解法**：引導階段目標早就鎖定了，我們已經知道主體是什麼。
空的 `placements` 更可能是「模型沒寫」而不是「東西不見了」。

現在只有模型**明確說 lost** 才當成不見，
否則沿用上一次已知的主體。鎖定當下就會記住，重新規劃時清掉。

### 3 · 模型判斷不了絕對亮度

**症狀**：畫面明顯偏暗，模型卻說光線沒問題。

**定位方法**：寫了 `scripts/check_stability.py` 做 A/B 對照——
同一組輸入，只換影像，各跑八次。

送一張**純白圖**（完全過曝）進去：

```
verdict 分布：{'ok': 8}
```

**8/8 說沒問題。** 模型對絕對亮度沒有參照基準，
這不是提示詞寫不好，調再多也不會準。

**解法**：本機算客觀數字，連同影像一起送給模型，
訊息裡直接寫上結論提示：

```
【亮度】
  主體 0.19 / 背景 0.41
  主體÷背景 = 0.46  → 主體明顯比背景暗
【反差】0.290  → 明暗落差大，屬於硬光
【顏色】色溫 -0.420　飽和度 0.510
  明顯偏冷（藍紫）、彩度很高，很可能是有色燈光、主色相偏藍
```

模型不再判斷「亮度夠不夠」，改為判斷
「這是什麼環境、光從哪來、該怎麼辦」——那才是它擅長的事。

**同一支腳本也證明了模型確實有在看圖：**
換成純白圖之後輸出改變了，而且各自八次完全一致。
如果兩組結果相同，就代表模型只是在讀 JSON 欄位，那整個設計要重來。

### 4 · 場景裡的強光壓過要拍的東西

**症狀**：在一個左側有強舞台燈的場地，模型一直說
「光從左邊來、右邊太暗、請從右側補光」，
使用者補了光也沒用。

**原因**：送給模型的曝光數字是**整張畫面**的左右上下。
那盞燈把左半邊拉爆，所以不管怎麼補光，結論都不會變。
模型忠實地在分析「整個場景」，不是使用者要拍的那個東西。

**解法**：把目標框傳進曝光統計，分開算主體與背景，
並算出 `subject_ratio`。訊息裡註明
「左右上下是整張畫面的，背景有強光時會失真，僅供參考」。

同時在提示詞開頭明確寫：

> 你判斷的是「使用者要拍的那個東西」的光線，不是整個場景。
> 畫面裡有強光源本身不是問題，只有當它讓要拍的東西看不清楚時才是。
> **不要評論人臉、人物或背景陳設。**

### 5 · 光線判定只有「好」和「壞」兩種

**症狀**：夜店的藍光、餐廳的暖黃燈被判定成「太暗」或「色偏」，
一直要求使用者補光。

**原因**：這是設計上的缺陷，不是 bug。
`verdict` 只有 ok 與各種問題，沒有「這是風格」這個選項。

把有氛圍的環境標成警告不只是煩，
**而且會讓使用者去消滅原本很好看的光。**

**解法**：`verdict` 改成三級。

| verdict | 意義 | 畫面呈現 |
| --- | --- | --- |
| `good` | 光線本身很好 | 灰底，說明光從哪來 |
| `stylish` | **有明顯風格** | **紫底**，教使用者怎麼「用」這個光 |
| `problem` | 真的看不清楚 | 橘紅底，給現場做得到的動作 |

另外加了 `env` 十種環境類型
（`neon` / `warm_indoor` / `golden_hour` / `lowlight` / `mixed` …），
先認出環境，再決定要不要修。

提示詞也明確允許「同時是風格、而且需要補一點光」：

> 「氣氛很好，但主體太暗，從正面補一點柔光就好」

並禁止「請使用專業補光設備」這類現場做不到的建議。

### 6 · 輸出 token 數決定延遲

**症狀**：延遲從早上的 306ms 惡化到中午常態逾時。

**排除過程**：先確認不是併發問題——
日誌顯示請求嚴格序列化，客戶端 port 從頭到尾相同，
同時只有一個請求在飛。所以平行拆成兩組連線不會變快，
只會讓同一個過載的端點負擔加倍。

**真正的原因**：一次要模型輸出太多東西——
版型判斷 + 版型 id + 微調參數 + 動態版型座標 + 物品配置
+ 移除清單 + 光線五欄位 + 構圖建議，`max_tokens=700`。

**解法**：按「變化速度」拆成三條路徑（見前述）。
主路徑的提示詞從約 2400 字元降到 729，`max_tokens` 從 700 降到 280。

**光線根本不需要每次都算**——房間的光幾秒內不會變，
但物品位置會。原本綁在一起是純浪費。

### 7 · 請求會堆積

**症狀**：後端變慢時，回應一回來馬上又送下一個，越擠越慢。

**原因**：計時基準是「送出之時」。
如果模型花了 2.5 秒，700ms 的間隔早就過完了。

**解法**：改成「收到回應並處理完之後」才開始計時。
節奏自動變成「延遲 + 間隔」，無論後端多慢都不會堆積。

### 8 · 畫布尺寸指數成長，把瀏覽器合成器搞垮

**症狀**：頁面開啟後正常，卡一下，然後整個畫面變白，
**而且 console 沒有任何錯誤**。

**原因**：`<canvas>` 是**替換元素（replaced element）**。
絕對定位的替換元素如果 CSS 沒有明確給 `width` / `height`，
會使用自己的**內在尺寸**（點陣寬高），不會被 `inset: 0` 撐開。

於是形成正回饋迴圈：

```
畫布預設 300×150
  → getBoundingClientRect() 讀到 300×150
  → 程式把點陣設成 300 × dpr
  → 點陣變了，內在尺寸跟著變，CSS 尺寸也變
  → ResizeObserver 觸發，再量一次
  → 600 × dpr → 1200 × dpr → 2400 × dpr → ...
```

dpr = 2 時八輪就從 300px 長到 76800px，約需 22 GB 記憶體。
瀏覽器在第四、五輪就撐不住，**顯示卡記憶體耗盡，合成器死亡**。

保險絲的實際訊息證實了推論：`畫布尺寸異常 4800x2400`——
從 300×150 連乘四次正好是這個數字，長寬比一模一樣。

**解法**：三道防線。

1. CSS 補上 `width: 100%; height: 100%`
2. **改成量「父容器」而不是畫布自己**——
   父容器的尺寸不會被畫布影響，迴圈從源頭就不存在，
   而且完全不依賴 CSS 有沒有寫對
3. 保險絲：單邊超過 4096px 或調整超過 40 次就停止並報錯

### 9 · 靜態檔案被硬快取，造成靜默失效

**症狀**：新版的光線分析「不見了」，
但程式碼明明有接好，而且沒有任何錯誤。

**定位方法**：看伺服器日誌的檔案清單——
`/js/lib/config.js` **完全沒有出現**。
沒出現代表瀏覽器連問都沒問就用了硬碟快取。

**原因**：FastAPI 的 `StaticFiles` 只送 `ETag` 與 `Last-Modified`，
**不送 `Cache-Control`**。瀏覽器遇到沒有 `Cache-Control` 的回應
會套用「啟發式快取」，自己決定一段時間內不重新驗證。

而新加的設定值在舊版 `config.js` 裡不存在：

```javascript
this.lastLightSent = performance.now() - undefined + 1200;  // → NaN
now - NaN > undefined                                       // → false，永遠不成立
```

**光線路徑一次都沒被排到，而且完全不報錯。**

**解法**：三道防線。

1. 自訂 `NoCacheStatic`，開發時一律送 `no-store`
2. 設定值改用 `cfg(key, fallback)` 取值，缺值退回內建預設
3. `config.js` 加 `BUILD` 版本戳記，
   console 印出實際生效的節奏，一眼看出載入的是不是舊檔

### 10 · 空值的型別會隨輸入影像改變

**症狀**：偶發的畫面異常，難以重現。

**發現方式**：`check_stability.py` 記錄了空值的實際型別。

```
A 組（場景圖）：空值型別 {'字串 "null"': 8}
B 組（純白圖）：空值型別 {'JSON null': 8}
```

**同一個模型、同一組提示詞，換一張圖，空值的型別就變了。**

字串 `"null"` 在 JavaScript 裡是**真值**，
前端會拿它去查物件，找不到，畫面就壞了。

**解法**：驗證層統一正規化
`"null"` / `"none"` / `"nil"` / `"n/a"` / `"-"` / `""` → `None`。

這種問題沒有做 A/B 對照測試不會發現，
而且它不會報錯，只會偶爾壞掉。

### 11 · 相機縮放查不到能力

**症狀**：明明是支援光學變焦的手機，
`track.getCapabilities()` 卻查不到 `zoom`。

**原因**：瀏覽器只有在 `getUserMedia` 的約束裡**就帶了 zoom**，
才會把它放進 capabilities。沒帶的話，即使硬體支援也查不到。

**但帶了可能被拒**：可能回 `OverconstrainedError`（這顆鏡頭不支援），
也可能回 `NotAllowedError`（使用者只允許一般相機）。

**解法**：先帶 `zoom: true` 試一次，失敗就退回不帶 zoom 再試一次，
第二次仍失敗才交給上層顯示真正的錯誤。

相容性：iOS Safari 完全不支援，桌機幾乎都不支援，Android 多數可以。
不支援時整個縮放介面隱藏。

### 12 · 多鏡頭列舉的時機

`enumerateDevices()` 在取得相機權限之前，
回傳的 `label` **全部是空字串**，而且可能連裝置都列不全。

所以列舉一定要排在成功 `getUserMedia` 之後。

另外，切換鏡頭一定要**先關掉舊串流**——
很多手機同時只能開一顆鏡頭，不先關會拿到 `NotReadableError`。

### 13 · 光線未知擋住了「可以拍攝」

**症狀**：構圖明明對好了，快門卻一直不變綠，
狀態列永遠停在「確認光線中」。

**原因**：

```javascript
const lightReady = !!light && light.verdict !== "problem";
```

`!!light` 是關鍵。光線分析改成「只在明顯變化時才呼叫」
之後，最短間隔 15 秒——所以光線還沒回來時 `lightReady` 就是 false。

**解法**：光線是輔助資訊，不是構圖完成的前提。
只有「明確判定有問題」才擋，未知一律視為可拍。
真的太暗時改成「構圖已到位・建議先補光」，快門仍然可以按。

---

## 容錯設計

系統的每一層都能單獨失效而不拖垮下一層。

| 失效點 | 結果 |
| --- | --- |
| 畫面沒變化 | 不送請求，畫面維持現狀 |
| 模型逾時 | 回 `fallback`，沿用目前的構圖目標 |
| 模型輸出不是 JSON | 撈大括號區塊再試；仍失敗則沿用上一個有效結果 |
| 動態版型不合格 | 先修座標；修不動才退回內建版型 |
| 個別 slot 有問題 | 只丟那一個 |
| 個別 placement 有問題 | 只丟那一筆 |
| 版型 id 無效但有 current | 沿用前端目前的版型，**光線建議仍然保住** |
| Provider 失敗 | 依序降級 國網 → GMI → OpenAI |
| 相機串流中斷 | 看門狗自動嘗試復原兩次，失敗才顯示錯誤 |
| 畫布尺寸異常 | 保險絲停止調整並在 console 報錯 |
| 設定值缺失 | 退回內建預設，不會變成 NaN |

**沒有任何路徑會讓畫面變成錯誤訊息或空白。**
最壞的情況是維持現狀。

倒數第二項值得單獨說明：光線建議跟版型判斷無關，
早期版本會因為版型 id 無效就丟掉整份回應，
連帶把光線建議也一起丟掉。現在兩者分開處理。

---

## 效能數字與測試

### 實測延遲

`scripts/bench_vlm.py` 會自行生成測試圖，
跑十次並輸出中位數與 P90，同時檢查 JSON 是否可解析。

```
模型: gemma-4-26B-A4B-it
影像: 512px 長邊
中位數 306 ms   P90 326 ms   最快 162   最慢 1370
JSON 可解析率 10/10
```

最慢的 1370ms 是冷啟動。所以前端在頁面載入時
會先打一次 `/api/warmup`，讓使用者的第一次真實請求
不是最慢的那次。

**尖峰時段實測過惡化到 2 秒以上。**
系統會自動放慢節奏並沿用現有目標，請求不會堆積，
但引導的即時感會下降。

### 節流的實際效果

除錯面板會顯示：

```
呼叫 plan 12 · light 3 · custom 0 · 略過 41
```

畫面變化偵測讓大部分的取樣不需要送出。
`light` 遠小於 `plan`，`custom` 大多是 0。

### 測試

```
JavaScript  86 項（node:test）
Python 合約 13 項（pytest）
```

涵蓋範圍：

- 13 個版型定義驗證、座標安全區、13 版型 × 5 種極端調整
- 前後端版型 id 與 slot 一致性
- 多幀共識：候選累積、固定目標、方向確認、ready 投票、lost 判定
- 畫面比對：手震容忍、亮度變化補償、場景更換、半徑可調
- 提示停留與立即中斷
- 曝光防線：偏暗與恢復的多幀確認、暗背景但主體清楚不誤判
- 光線變化觸發、動態版型修正、自相矛盾壓制、舊格式相容
- 兩指縮放：倍率夾範圍、只保留最新目標值、不支援時不觸發
- 介面開關：後端覆寫、壞掉的回應不弄壞開關

測試中有兩處刻意改寫過既有斷言，
因為它們編碼的正是後來要反轉的行為
（「READY 要等光線」、「250ms 內宣告移動」）。
兩處都在測試檔的註解裡寫清楚了為什麼改，
避免之後的人以為是被改壞的。

---

## 附錄：可調參數

全部集中在 `web/js/lib/config.js` 與 `.env`。

```javascript
PLAN_INTERVAL_MS: 1000       // 構圖請求的目標起始間隔
SCAN_INTERVAL_MS: 250        // 本機畫面取樣間隔
GUIDANCE_HOLD_MS: 7000       // 明確指示的最短顯示時間
MOTION: { threshold: 16, radius: 3, confirmations: 2 }
LIGHT_CHECK_INTERVAL_MS: 1000
LIGHT_CHANGE: { confirmations: 3, minIntervalMs: 15000, ... }
LIGHT_READABILITY: { darkMean: .28, darkSubject: .34, ... }
STABILITY: { planConfirmations: 2, readyVotes: 2, readyWindow: 3, ... }
```

`.env` 的介面開關（改完重啟 uvicorn 即生效）：

```bash
SHOW_ANALYSIS_STATUS=false   # 判斷狀態列
ALLOW_DEBUG_PANEL=true       # 按 D 的除錯面板
PINCH_ZOOM=true              # 兩指縮放；false 退回滑桿
```
